apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ app }}-config
  labels:
    app: {{ app }}
data:
  POSTGRES_DB: {{ pg_database }}
  POSTGRES_USER: {{ pg_user }}
  POSTGRES_PASSWORD: {{ pg_pass }}
  VERSION: {{ version }}