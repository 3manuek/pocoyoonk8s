---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ app }}-config
  labels:
    app: {{ app }}
data:
  POSTGRES_DB: "{{ pg_database }}"
  POSTGRES_USER: "{{ pg_user }}"
  VERSION: "{{version}}"
  APP: "{{app}}"