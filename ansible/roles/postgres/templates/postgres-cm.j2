apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ component }}-config
  labels:
    app: {{ component }}
data:
  POSTGRES_DB: {{ pg_database }}
  POSTGRES_USER: {{ pg_user }}
  POSTGRES_PASSWORD: {{ pg_pass }}
  VERSION: {{ version }}
