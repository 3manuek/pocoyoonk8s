apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ app_name }}-config
  labels:
    app: {{ app_name }}
data:
  POSTGRES_DB: {{ pg_database }}
  POSTGRES_USER: {{ pg_user }}
  POSTGRES_PASSWORD: {{ pg_pass }}
